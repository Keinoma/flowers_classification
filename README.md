The folders can be very similar so there is clearly a more clever way to assemble them to avoid some repetitions.
But it is more understandable in the curent version.


Check architecture.txt to see it more clearly. The differences between the different folders are described in ROADMAP.mp


Finally, there is a script called gradcam.py that can be used in pretty much every folder, it's not necessary but it can be useful during debug. There is only a few names of paths tht should be adapted depending on which folder do we run it from, but it is really quick to do.
